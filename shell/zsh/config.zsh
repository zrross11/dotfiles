# Configuration file for dotfiles
# Set these variables to enable/disable features

# Kubernetes context and namespace display in Starship prompt
# Set to 1 to enable, 0 to disable
export DOTFILES_SHOW_KUBE_CONTEXT=${DOTFILES_SHOW_KUBE_CONTEXT:-1}

# Additional configuration can be added here
# Example: export DOTFILES_FEATURE_X=1

